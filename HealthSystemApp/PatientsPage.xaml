<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HealthSystemApp.PatientsPage"
             Title="Patients">
    <VerticalStackLayout Padding="20">
        <Button Text="Add Patient" Clicked="OnAddPatients"/>
        <CollectionView x:Name="PatientsList" ItemsSource="{Binding Patients}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Label Text="Name" FontSize="16"/>
                        <Button Text="Edit" Clicked="OnEditPatient"/>
                        <Button Text="Delete" Clicked="OnDeletePatient"/>
                        <Button Text="Add note" Clicked="OnAddNote"/>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
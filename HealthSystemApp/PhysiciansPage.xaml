<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HealthSystemApp.PhysiciansPage"
             Title="Physicians">
    <ScrollView>


        <VerticalStackLayout Padding="20" Spacing="30">
            <Image Source="physician.png" Aspect="AspectFit" HeightRequest="300"/>
            <Button Text="Add Physician" Clicked="OnAddPhysician"/>
            <CollectionView x:Name="PhysiciansList" ItemsSource="{Binding Physicians}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal" Padding="5">
                            <Label Text="{Binding name}" FontSize="16"/>
                            <Button Text="Edit" Clicked="OnEditPhysician"/>
                            <Button Text="Delete" Clicked="OnDeletePhysician"/>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>